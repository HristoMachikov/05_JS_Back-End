<h1>Attach a new accessory</h1>
<div class="form">
    <h2>{{cube.name}}</h2>
    <img class="cube" src="{{cube.imageUrl}}">
     {{#if accessoaries}}
    <form action='/attach/accessoary/{{cube._id}}' method="POST">
        <label for="accessoary">Accessories</label>
        <select id="accessoary" name="accessoary">
            {{#each accessoaries}}
            <option value="{{this._id}}">{{this.name}}</option>
            {{/each}}
        </select>
        <input type="submit" value="Attach">
    </form>
    {{else}}
    <h3 class="italic">This cube has all available accessories so far or there is no available at all...</h3>
    {{/if}}
    <a class="btn" href="/">Back</a>
</div>